#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  double t;
  cin >> t;
  cout << fixed << setprecision(2) << t / (1 / 22.0 + 1 / 30.0) << '\n';
}
