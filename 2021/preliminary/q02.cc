#include <bits/stdc++.h>

using namespace std;
using ll = long long;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  double h;
  cin >> h;
  cout << fixed << setprecision(2) << sqrt(2 * h / 9.8) << '\n';
}
